name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

job:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      -name: build
        run: make build